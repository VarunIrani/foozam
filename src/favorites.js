/* eslint-disable no-alert */
/* eslint-disable no-undef */
import $ from 'jquery';

function loadRecipes() {}

function loadRestaurants() {}

$(document).ready(() => {
  alert('favorites.js');
  loadRecipes();
  loadRestaurants();
  // loadBlogs();
});
